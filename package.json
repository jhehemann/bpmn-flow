{
  "name": "bpmn-flow",
  "private": true,
  "type": "module",
  "description": "Iterative BPMN editing with an AI coding assistant; live preview via browser viewer",
  "scripts": {
    "layout": "node scripts/layout.mjs",
    "validate": "bpmnlint flows/*.bpmn"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "bpmn-auto-layout": "^1.3.0",
    "bpmnlint": "^11.6.0"
  }
}
